# AWS Lightsail Moodle Deployment Configuration
# Copy to .env and update with your values
# This is for production AWS deployment

# ============================================
# RDS Database Configuration
# ============================================
# Get these values from AWS RDS console after creating the database

# RDS endpoint WITHOUT the port (e.g., moodle-db.c9akciq32.us-east-1.rds.amazonaws.com)
RDS_ENDPOINT=moodle-db.xxxxx.us-east-1.rds.amazonaws.com

# Database name (created in RDS)
DB_NAME=moodle

# Database user (created in RDS)
DB_USER=moodle

# Strong password (MUST be different from local password)
# Requirements: 8+ chars, uppercase, lowercase, numbers, special chars
DB_PASSWORD=GenerateStrongPassword123!

# ============================================
# Moodle Site Configuration
# ============================================

# Your domain URL (with https://)
MOODLE_URL=https://moodle.yourdomain.com

# Site name - will be displayed in Moodle
SITE_NAME=My Moodle Site

# Admin email for notifications
ADMIN_EMAIL=admin@yourdomain.com

# ============================================
# AWS Lightsail Configuration
# ============================================

# Lightsail instance region (must match RDS region)
AWS_REGION=us-east-1

# Lightsail SSH key file path (local reference, not uploaded)
# SSH_KEY_PATH=/path/to/lightsail-key.pem

# ============================================
# Optional: SSL/HTTPS Configuration
# ============================================

# Let's Encrypt certificate domain
# LETSENCRYPT_DOMAIN=moodle.yourdomain.com

# Let's Encrypt email for certificate renewal notices
# LETSENCRYPT_EMAIL=admin@yourdomain.com

# ============================================
# Optional: Backup Configuration
# ============================================

# S3 bucket for backups (if using AWS S3)
# AWS_BACKUP_BUCKET=moodle-backups-bucket

# Backup frequency (daily, weekly, monthly)
# BACKUP_FREQUENCY=daily

# ============================================
# Optional: Performance Tuning
# ============================================

# Maximum memory limit for PHP
# PHP_MAX_MEMORY=256M

# Maximum upload size
# UPLOAD_MAX_FILESIZE=100M

# ============================================
# IMPORTANT SECURITY NOTES
# ============================================
# 1. NEVER commit this .env file to git
# 2. Use strong, unique passwords
# 3. Keep RDS password separate from local password
# 4. Store this file securely
# 5. Rotate passwords every 90 days
# 6. Enable RDS encryption at rest
# 7. Enable Lightsail firewall
# 8. Use SSH keys instead of passwords for server access
